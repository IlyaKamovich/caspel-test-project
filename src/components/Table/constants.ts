import type { ITableOptions } from './types';

const TABLE_OPTIONS: ITableOptions = {
	PAGINATION: {
		PAGE_SIZE: 10,
	},
	SIZE: 'middle',
	EMPTY_TABLE_DESCRIPTION: 'Нет данных',
	SCROLL: { x: 'max-content' },
};

export { TABLE_OPTIONS };
